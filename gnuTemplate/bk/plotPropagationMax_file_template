set output "CCOUTFILE.eps"
set terminal postscript eps enhanced color font "Tahoma,14"
set ylabel "Max propagation delay"
set xlabel "Delay [sec]"
set title "Propagation max vs upload bandwidth and peer selection"